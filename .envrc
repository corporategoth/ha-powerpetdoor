# direnv configuration for automatic venv activation.
# Run 'direnv allow' to enable this file.

if [ -d .venv ]; then
    source .venv/bin/activate
else
    echo "Virtual environment not found. Run './scripts/setup-dev.sh' to create it."
fi
